# ============================================================
# Wednesday AI Assistant — Requirements
# Phase 1: Voice pipeline + AI conversation loop
# ============================================================
# Install: pip install -r requirements.txt
# Requires Python 3.11+
# ============================================================

# ── Core / Config ───────────────────────────────────────────
pydantic>=2.5.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# ── LLM Providers ───────────────────────────────────────────
openai>=1.35.0
anthropic>=0.28.0

# ── HTTP client (ElevenLabs streaming) ──────────────────────
httpx>=0.27.0

# ── Speech-to-Text ──────────────────────────────────────────
faster-whisper>=1.0.1

# ── Audio I/O ──────────────────────────────────────────────
PyAudio>=0.2.14
sounddevice>=0.4.7
numpy>=1.26.0
scipy>=1.12.0

# ── Wake Word Detection ─────────────────────────────────────
pvporcupine>=3.0.2

# ── Text-to-Speech (local fallback) ─────────────────────────
pyttsx3>=2.90

# ── Audio playback (for ElevenLabs) ─────────────────────────
pygame>=2.5.2

# ── Memory / Vector DB ──────────────────────────────────────
chromadb>=0.5.3
sentence-transformers>=3.0.1

# ── System tray UI ──────────────────────────────────────────
pystray>=0.19.5
Pillow>=10.3.0

# ── Desktop control (Phase 2) ───────────────────────────────
pyautogui>=0.9.54
pygetwindow>=0.0.9
psutil>=5.9.8
keyboard>=0.13.5

# ── Windows-specific ────────────────────────────────────────
pywin32>=306; sys_platform == "win32"
comtypes>=1.3.1; sys_platform == "win32"

# ── Testing ─────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
